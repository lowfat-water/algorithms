#include <iostream>
#include <vector>

template <typename T>
void join(vector<T> &vA, const vector<T> &vB)
{
    int sizeB = vB.size();
    for(int i=0; i<sizeB; i++)
        vA.push_back(vB[i])
}

int main
{
    
}
